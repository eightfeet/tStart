<!DOCTYPE html>
<html>
    <head>
        <% for (var chunk in htmlWebpackPlugin.files.css) { %>
        <link
            rel="preload"
            href="<%= htmlWebpackPlugin.files.css[chunk] %>"
            as="style"
        />
        <% } %> <% for (var chunk in htmlWebpackPlugin.files.chunks) { %>
        <link
            rel="preload"
            href="<%= htmlWebpackPlugin.files.chunks[chunk].entry %>"
            as="script"
        />
        <% } %>
        <meta charset="utf-8" />
        <title><%= htmlWebpackPlugin.options.BY_HEALTH_PAGETITLE %></title>
        <meta http-equiv="Cache-Control" content="no-siteapp" />
        <meta
            name="viewport"
            content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=no"
        />
        <meta name="description" content="activitiesCli-by-health" />
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta
            name="apple-mobile-web-app-status-bar-style"
            content="black-translucent"
        />
        <meta name="apple-mobile-web-app-title" content="<%= htmlWebpackPlugin.options.BY_HEALTH_PAGETITLE %>" />
        <meta name="application-name" content="<%= htmlWebpackPlugin.options.BY_HEALTH_PAGETITLE %>" />
        <link
            rel="shortcut icon"
            href="<%= htmlWebpackPlugin.options.PUBLIC_PATH %>assets/favicon.ico"
        />
        <meta name="format-detection" content="telephone=no" />
    </head>
    <body>
        <div id="root"></div>
    </body>
</html>
