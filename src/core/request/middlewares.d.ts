import { Middleware } from './typings';
export declare const baseUrl: Middleware;
export declare const params: Middleware;
export declare const type: Middleware;
export declare const http: Middleware<Response>;
export declare const json: Middleware;
export declare const timeout: Middleware;
